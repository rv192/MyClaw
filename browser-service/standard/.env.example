# ============================================
# OpenClaw Browser Service - Standard Version
# Environment Configuration
# ============================================

# ============================================
# 显示器配置
# ============================================
DISPLAY=:99
SCREEN_WIDTH=1920
SCREEN_HEIGHT=1080
SCREEN_DEPTH=24

# ============================================
# 远程访问配置
# ============================================
# 远程访问模式
# 0 = 关闭（仅浏览器 API）
# 1 = VNC
# 2 = RustDesk
# 3 = VNC + RustDesk
REMOTE_TYPE=0

# VNC 端口（仅 REMOTE_TYPE=1 或 3 时生效）
VNC_PORT=5900

# RustDesk 端口（仅 REMOTE_TYPE=2 或 3 时生效）
RUSTDESK_PORT=5900

# 共用密码
REMOTE_PASSWORD=openclaw123

# ============================================
# 可选：Chrome 安装
# ============================================
# 如需安装真正的 Google Chrome，请在 Dockerfile 中添加：
# RUN wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | apt-key add - \
#     && echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list \
#     && apt-get update \
#     && apt-get install -y google-chrome-stable \
#     && rm -rf /var/lib/apt/lists/*

# ============================================
# 注意事项
# ============================================
# 1. 标准版默认 REMOTE_TYPE=0（仅浏览器 API）
# 2. 如需远程访问，请设置 REMOTE_TYPE=1、2 或 3
# 3. RustDesk 已默认安装，通过 REMOTE_TYPE 控制启动
# 4. 生产环境建议使用密码文件而非明文密码
# 5. Camoufox 已自动安装，无需额外配置