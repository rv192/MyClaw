# ============================================
# OpenClaw Browser Service - Minimal Version
# Environment Configuration
# ============================================

# ============================================
# 显示器配置
# ============================================
DISPLAY=:99
SCREEN_WIDTH=1920
SCREEN_HEIGHT=1080
SCREEN_DEPTH=24

# ============================================
# 远程访问配置
# ============================================
# 远程访问模式
# 0 = 关闭（仅浏览器 API）
# 1 = VNC（默认）
# 2 = RustDesk（极简版不支持）
# 3 = VNC + RustDesk（极简版不支持）
REMOTE_TYPE=1

# VNC 端口（仅 REMOTE_TYPE=1 或 3 时生效）
VNC_PORT=5900

# RustDesk 端口（仅 REMOTE_TYPE=2 或 3 时生效）
RUSTDESK_PORT=5900

# 共用密码
REMOTE_PASSWORD=openclaw123

# ============================================
# 注意事项
# ============================================
# 1. 极简版不支持 RustDesk，REMOTE_TYPE=2 和 3 会报错
# 2. 建议使用 REMOTE_TYPE=1 启用 VNC
# 3. 生产环境建议使用密码文件而非明文密码